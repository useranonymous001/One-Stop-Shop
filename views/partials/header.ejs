<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="font-['helvetica_now_display']">
    <nav
      class="w-full fixed top-0 left-0 bg-white shadow-md px-5 py-3 flex items-center justify-between font-sans font-lg font-bold"
    >
      <!-- <h3 class="text-2xl font-bold text-blue-600">LaziStore</h3> -->
      <img src="../../images/logo.png" class="h-10" alt="" />

      <% var loggedIn = typeof loggedIn !== "undefined" ? loggedIn : true %> <%
      if(loggedIn) { %>
      <div class="flex gap-5 items-center">
        <a href="">Home &#127968;</a>
        <a href="/shop">Products &#x1F6CD;</a>
        <a href="/cart"> Cart &#128722;</a>
        <a href="/users/logout" class="text-red-500">Logout &duarr;</a>
        <a href="/profile">Account &#x1F5B3;</a>

        <div class="profile-pic w-10 overflow-hidden h-10 rounded-full ml-3">
          <img
            class="object-cover w-full h-full object-center"
            src="/images/uploads/<%= user.profilepic %>"
            alt="no image"
          />
        </div>
      </div>

      <% } %>
    </nav>
  </body>
</html>
