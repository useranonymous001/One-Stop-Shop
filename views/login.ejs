<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lazi Store</title>

    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900">
    <% if (message.length > 0) { %>
    <div
      class="fixed top-0 left-1/2 transform -translate-x-1/2 mt-4 bg-red-500 text-white px-4 py-2 rounded shadow-md"
    >
      <%= message %>
    </div>
    <% } %>

    <div
      class="w-full min-h-screens mb-10 flex flex-col items-center px-4 lg:px-20 font-sans text-white"
    >
      <div
        class="w-full flex flex-col items-center justify-center mt-16 lg:mt-20"
      >
        <h1 class="text-4xl lg:text-6xl font-bold text-blue-400">
          Welcome to LaziStore
        </h1>
        <p
          class="font-semibold flex justify-center items-center mt-2 text-center"
        >
          Your one-stop shop for all cybersecurity tools and gadgets.
        </p>
      </div>
      <div
        class="w-full flex flex-col lg:flex-row justify-center items-center lg:items-start lg:mt-16 mt-8 space-y-8 lg:space-y-0 lg:space-x-16"
      >
        <div class="w-full lg:w-1/2 flex items-center justify-center">
          <div class="w-full px-4 lg:px-32">
            <h4 class="text-2xl capitalize mb-5">login your account</h4>
            <form autocomplete="off" action="/users/login" method="post">
              <input
                class="block bg-zinc-100 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                type="email"
                placeholder="Email"
                name="email"
              />
              <input
                class="block bg-zinc-100 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                type="password"
                placeholder="Password"
                name="password"
              />
              <div class="btns flex items-center justify-between">
                <input
                  class="px-5 rounded-md py-2 mt-2 bg-blue-500 font-bold text-white cursor-pointer hover:bg-blue-800 transition duration:300 ease-in-out"
                  type="submit"
                  value="Login"
                />

                <p>
                  New User?
                  <a href="/" class="text-blue-400 underline"
                    >Create New Account
                  </a>
                </p>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
